# hilicurl

```
Usage: ./hilicurl URL
  -h    Shorthand for -help
  -help
        Print help
  -interval duration
        Interval between each request (default 2s)
  -timeout duration
        Request timeout (default 1m0s)
```
